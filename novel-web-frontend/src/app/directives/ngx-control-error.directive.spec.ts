import { NgxControlErrorDirective } from './ngx-control-error.directive';

describe('NgxControlErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxControlErrorDirective();
    expect(directive).toBeTruthy();
  });
});
